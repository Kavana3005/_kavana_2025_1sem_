#include <stdio.h>
#include <math.h>
int  main() int a, b, product;
{
    prinf("enter the first value a");
    scanf("%d", &a);
    pprintf("enter the value of b");
    scanf("%d", &b);
    product = a * b;
    printf("product of %d and %d;%d\n", a, b, product);
}